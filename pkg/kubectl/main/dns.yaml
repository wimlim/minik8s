apiVersion: v1
kind: Dns
metadata:
  name: example-dns
  namespace: default
spec:
  host: example-service
  paths:
    - subPath: /python
      serviceIp: 3.3.3.232  # 这里需要填入实际的Service ClusterIP
      serviceName: example-service
      servicePort: 4335