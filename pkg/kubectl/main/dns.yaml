apiVersion: v1
kind: Dns
metadata:
  name: example-dns
  namespace: default
spec:
  host: example-service
  paths:
    - subPath: /path
      serviceIp: 3.3.3.244  # 这里需要填入实际的Service ClusterIP
      serviceName: example-service
      servicePort: 4335